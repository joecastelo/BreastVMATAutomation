<UserControl x:Class="BreastVMATAutomation.UI.ConfigContexts.ConfigOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BreastVMATAutomation.UI.ConfigContexts"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <Button Style="{x:Null}" Margin="10" x:Name="LINACCalcDefault" Click="LINACCalcDefault_Click"  Content="Calculation Default"></Button>
            <Button Style="{x:Null}" Margin="10" x:Name="BreastCLAlias" Click="BreastCLAlias_Click"  Content="Structure Naming"></Button>

            <Button Style="{x:Null}" Margin="10" x:Name="ArcSetupConfig" Click="ArcSetupConfig_Click" Content="Arc Setup Config"></Button>


        </DockPanel>
    </Grid>
</UserControl>
